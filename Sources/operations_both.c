#include "ps.h"

void	ss(t_system *sys, char **str)
{
	sa(sys, str);
	sb(sys, str);
}

void	rr(t_system *sys, char **str)
{
	ra(sys, str);
	rb(sys, str);
}

void	rrr(t_system *sys, char **str)
{
	rra(sys, str);
	rrb(sys, str);
}
